
In ./tests/validation-pipeline-e2e.test.sh line 83:
long_running_command='for i in 1 2 3 4; do echo "stdout tick $i"; echo "stderr tick $i" >&2; sleep 0.6; done; echo "POST_TIMEOUT_STDOUT marker"; echo "POST_TIMEOUT_STDERR marker" >&2; exit 7'
                     ^-- SC2016 (info): Expressions don't expand in single quotes, use double quotes for that.


In ./tests/github-private-key-metadata.test.sh line 68:
node -e '
        ^-- SC2016 (info): Expressions don't expand in single quotes, use double quotes for that.


In ./tests/github-preflight-helper-load.test.sh line 56:
KASEKI_GITHUB_PREFLIGHT_AUTH_CHECK=0
^-- SC2034 (warning): KASEKI_GITHUB_PREFLIGHT_AUTH_CHECK appears unused. Verify use (or export if used externally).


In ./tests/npm-install-flags.test.sh line 27:
  KASEKI_INSTALL_IGNORE_SCRIPTS="$ignore_scripts"
  ^---------------------------^ SC2034 (warning): KASEKI_INSTALL_IGNORE_SCRIPTS appears unused. Verify use (or export if used externally).


In ./tests/github-askpass-exec.test.sh line 29:
  chmod() { return 1; }
            ^------^ SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-preflight-auth.test.sh line 69:
   ! grep -Fq 'create_github_askpass_helper "$health_log"' "$HEALTH_SOURCE" || \
              ^-- SC2016 (info): Expressions don't expand in single quotes, use double quotes for that.


In ./tests/github-preflight-auth.test.sh line 77:
ASKPASS_CHECK_LINE="$(grep -nF 'create_github_askpass_helper "$health_log"' "$HEALTH_SOURCE" | head -n 1 | cut -d: -f1)"
                               ^-- SC2016 (info): Expressions don't expand in single quotes, use double quotes for that.


In ./tests/github-preflight-auth.test.sh line 87:
   ! grep -Fq '[ -z "$password_smoke_output" ]' "$ASKPASS_SOURCE"; then
              ^-- SC2016 (info): Expressions don't expand in single quotes, use double quotes for that.


In ./tests/github-preflight-auth.test.sh line 101:
KASEKI_GITHUB_PREFLIGHT_AUTH_CHECK=1
^-- SC2034 (warning): KASEKI_GITHUB_PREFLIGHT_AUTH_CHECK appears unused. Verify use (or export if used externally).


In ./tests/github-preflight-auth.test.sh line 121:
node -e '
        ^-- SC2016 (info): Expressions don't expand in single quotes, use double quotes for that.


In ./tests/github-app-token-install-layout.test.sh line 40:
node -e '
        ^-- SC2016 (info): Expressions don't expand in single quotes, use double quotes for that.


In ./tests/repo-memory.test.sh line 20:
GIT_REF="main"
^-----^ SC2034 (warning): GIT_REF appears unused. Verify use (or export if used externally).


In ./tests/repo-memory.test.sh line 21:
TASK_PROMPT="Fix the widget parser."
^---------^ SC2034 (warning): TASK_PROMPT appears unused. Verify use (or export if used externally).


In ./tests/repo-memory.test.sh line 22:
KASEKI_AGENT_GUARDRAILS=1
^---------------------^ SC2034 (warning): KASEKI_AGENT_GUARDRAILS appears unused. Verify use (or export if used externally).


In ./tests/repo-memory.test.sh line 23:
KASEKI_REPO_MEMORY_MODE=summary
^---------------------^ SC2034 (warning): KASEKI_REPO_MEMORY_MODE appears unused. Verify use (or export if used externally).


In ./tests/repo-memory.test.sh line 24:
KASEKI_REPO_MEMORY_TTL_DAYS=30
^-------------------------^ SC2034 (warning): KASEKI_REPO_MEMORY_TTL_DAYS appears unused. Verify use (or export if used externally).


In ./tests/repo-memory.test.sh line 25:
KASEKI_REPO_MEMORY_MAX_BYTES=4096
^--------------------------^ SC2034 (warning): KASEKI_REPO_MEMORY_MAX_BYTES appears unused. Verify use (or export if used externally).


In ./tests/repo-memory.test.sh line 26:
KASEKI_REPO_MEMORY_ROOT="$TMP_DIR/repo-memory"
^---------------------^ SC2034 (warning): KASEKI_REPO_MEMORY_ROOT appears unused. Verify use (or export if used externally).


In ./tests/repo-memory.test.sh line 28:
KASEKI_DRY_RUN=0
^------------^ SC2034 (warning): KASEKI_DRY_RUN appears unused. Verify use (or export if used externally).


In ./tests/repo-memory.test.sh line 29:
REPO_MEMORY_KEY=""
^-------------^ SC2034 (warning): REPO_MEMORY_KEY appears unused. Verify use (or export if used externally).


In ./tests/repo-memory.test.sh line 32:
REPO_MEMORY_STATUS="disabled"
^----------------^ SC2034 (warning): REPO_MEMORY_STATUS appears unused. Verify use (or export if used externally).


In ./tests/repo-memory.test.sh line 88:
PI_EXIT=0
^-----^ SC2034 (warning): PI_EXIT appears unused. Verify use (or export if used externally).


In ./tests/repo-memory.test.sh line 89:
SECRET_SCAN_EXIT=0
^--------------^ SC2034 (warning): SECRET_SCAN_EXIT appears unused. Verify use (or export if used externally).


In ./tests/repo-memory.test.sh line 91:
KASEKI_TASK_MODE=patch
^--------------^ SC2034 (warning): KASEKI_TASK_MODE appears unused. Verify use (or export if used externally).


In ./tests/repo-memory.test.sh line 92:
START_ISO="2026-05-06T12:00:00Z"
^-------^ SC2034 (warning): START_ISO appears unused. Verify use (or export if used externally).


In ./tests/repo-memory.test.sh line 93:
VALIDATION_EXIT=0
^-------------^ SC2034 (warning): VALIDATION_EXIT appears unused. Verify use (or export if used externally).


In ./tests/repo-memory.test.sh line 94:
QUALITY_EXIT=0
^----------^ SC2034 (warning): QUALITY_EXIT appears unused. Verify use (or export if used externally).


In ./tests/github-operations.test.sh line 24:
} && pass "printf with dash-containing reasons succeeded" || fail "printf with dash-containing reasons failed"
  ^-- SC2015 (info): Note that A && B || C is not if-then-else. C may run when A is true.


In ./tests/github-operations.test.sh line 52:
} && pass "Multi-argument printf succeeded" || fail "Multi-argument printf failed"
  ^-- SC2015 (info): Note that A && B || C is not if-then-else. C may run when A is true.


In ./tests/github-operations.test.sh line 66:
} && pass "printf with empty array succeeded" || fail "printf with empty array failed"
  ^-- SC2015 (info): Note that A && B || C is not if-then-else. C may run when A is true.


In ./tests/github-operations.test.sh line 79:
} && pass "printf with special chars in URL succeeded" || fail "printf with special chars in URL failed"
  ^-- SC2015 (info): Note that A && B || C is not if-then-else. C may run when A is true.


In ./tests/github-operations.test.sh line 93:
} && pass "printf with dash-containing branch name succeeded" || fail "printf with dash-containing branch name failed"
  ^-- SC2015 (info): Note that A && B || C is not if-then-else. C may run when A is true.


In ./tests/github-operations.test.sh line 130:
) && pass "Actual skip reasons logic survived errexit" || fail "Actual skip reasons logic failed under errexit"
  ^-- SC2015 (info): Note that A && B || C is not if-then-else. C may run when A is true.


In ./tests/github-operations.test.sh line 311:
if grep -q 'git_push_exit="${PIPESTATUS\[0\]:-1}"' "$ROOT_DIR/kaseki-agent.sh" && \
           ^-- SC2016 (info): Expressions don't expand in single quotes, use double quotes for that.


In ./tests/github-operations.test.sh line 320:
if grep -q 'https://api.github.com/repos/$owner/$repo/issues/$issue_number/labels' "$ROOT_DIR/kaseki-agent.sh"; then
           ^-- SC2016 (info): Expressions don't expand in single quotes, use double quotes for that.


In ./tests/github-operations.test.sh line 339:
if grep -q 'apply_github_pr_labels "$owner" "$repo" "$pr_number" "$token" /results/git-push.log || true' "$ROOT_DIR/kaseki-agent.sh" && \
           ^-- SC2016 (info): Expressions don't expand in single quotes, use double quotes for that.


In ./tests/dependency-cache-key.test.sh line 23:
KASEKI_INSTALL_IGNORE_SCRIPTS=1
^---------------------------^ SC2034 (warning): KASEKI_INSTALL_IGNORE_SCRIPTS appears unused. Verify use (or export if used externally).


In ./tests/dependency-cache-key.test.sh line 30:
REPO_URL="https://example.com/project.git" GIT_REF="feature-b"
^------^ SC2034 (warning): REPO_URL appears unused. Verify use (or export if used externally).
                                           ^-----^ SC2034 (warning): GIT_REF appears unused. Verify use (or export if used externally).


In ./tests/dependency-cache-key.test.sh line 44:
KASEKI_NPM_OMIT_DEV=1
^-----------------^ SC2034 (warning): KASEKI_NPM_OMIT_DEV appears unused. Verify use (or export if used externally).


In ./tests/github-operations-failures.test.sh line 28:
  local expected="$1"
  ^-----------------^ SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 29:
  local actual="$2"
  ^---------------^ SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 30:
  local test_desc="$3"
  ^------------------^ SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 32:
  if [ "$actual" -eq "$expected" ]; then
  ^-- SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).
     ^---------------------------^ SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 33:
    printf '%b✓%b %s (exit %d)\n' "$GREEN" "$NC" "$test_desc" "$actual"
    ^-- SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 34:
    ((TESTS_PASSED++))
    ^----------------^ SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 35:
    return 0
    ^------^ SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 37:
    printf '%b✗%b %s - expected exit %d, got %d\n' "$RED" "$NC" "$test_desc" "$expected" "$actual"
    ^-- SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 38:
    ((TESTS_FAILED++))
    ^----------------^ SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 39:
    return 1
    ^------^ SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 44:
  local file="$1"
  ^-------------^ SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 45:
  local pattern="$2"
  ^----------------^ SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 46:
  local test_desc="$3"
  ^------------------^ SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 48:
  if [ ! -f "$file" ]; then
  ^-- SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).
     ^--------------^ SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 49:
    printf '%b✗%b %s - file not found: %s\n' "$RED" "$NC" "$test_desc" "$file"
    ^-- SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 50:
    ((TESTS_FAILED++))
    ^----------------^ SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 51:
    return 1
    ^------^ SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 54:
  if grep -q "$pattern" "$file"; then
  ^-- SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).
     ^------------------------^ SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 55:
    printf '%b✓%b %s\n' "$GREEN" "$NC" "$test_desc"
    ^-- SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 56:
    ((TESTS_PASSED++))
    ^----------------^ SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 57:
    return 0
    ^------^ SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 59:
    printf '%b✗%b %s - pattern not found in %s\n' "$RED" "$NC" "$test_desc" "$file"
    ^-- SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 60:
    ((TESTS_FAILED++))
    ^----------------^ SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 61:
    return 1
    ^------^ SC2317 (info): Command appears to be unreachable. Check usage (or ignore if invoked indirectly).


In ./tests/github-operations-failures.test.sh line 206:
if grep -q 'token_exit_code=\$?' "$PROJECT_ROOT/kaseki-agent.sh" && grep -q 'github-app-token.*>"\$token_stdout_tmp" 2>"\$token_stderr_tmp"' "$PROJECT_ROOT/kaseki-agent.sh"; then
                                                                            ^-- SC2016 (info): Expressions don't expand in single quotes, use double quotes for that.


In ./tests/github-operations-failures.test.sh line 254:
if grep -q 'GitHub App token generation failed (exit code \$GITHUB_PUSH_EXIT)' "$PROJECT_ROOT/kaseki-agent.sh"; then
           ^-- SC2016 (info): Expressions don't expand in single quotes, use double quotes for that.


In ./tests/github-operations-failures.test.sh line 369:
if grep -q 'GITHUB_APP_ENABLED="\${GITHUB_APP_ENABLED:-1}"' "$PROJECT_ROOT/run-kaseki.sh"; then
           ^-- SC2016 (info): Expressions don't expand in single quotes, use double quotes for that.


In ./tests/github-operations-failures.test.sh line 377:
if grep -q 'GITHUB_APP_ENABLED="\${GITHUB_APP_ENABLED:-1}"' "$PROJECT_ROOT/kaseki-agent.sh"; then
           ^-- SC2016 (info): Expressions don't expand in single quotes, use double quotes for that.


In ./tests/pr-metadata-generation.test.sh line 66:
KASEKI_MODEL='openrouter/test-model'
^----------^ SC2034 (warning): KASEKI_MODEL appears unused. Verify use (or export if used externally).


In ./tests/pr-metadata-generation.test.sh line 67:
ACTUAL_MODEL='openrouter/actual-model'
^----------^ SC2034 (warning): ACTUAL_MODEL appears unused. Verify use (or export if used externally).


In ./tests/pr-metadata-generation.test.sh line 68:
START_EPOCH=$(($(date +%s) - 125))
^---------^ SC2034 (warning): START_EPOCH appears unused. Verify use (or export if used externally).


In ./tests/pr-metadata-generation.test.sh line 69:
PRE_VALIDATION_EXIT=0
^-----------------^ SC2034 (warning): PRE_VALIDATION_EXIT appears unused. Verify use (or export if used externally).


In ./tests/pr-metadata-generation.test.sh line 71:
QUALITY_EXIT=0
^----------^ SC2034 (warning): QUALITY_EXIT appears unused. Verify use (or export if used externally).


In ./tests/pr-metadata-generation.test.sh line 72:
SECRET_SCAN_EXIT=0
^--------------^ SC2034 (warning): SECRET_SCAN_EXIT appears unused. Verify use (or export if used externally).


In ./tests/pr-metadata-generation.test.sh line 104:
KASEKI_RESULTS_DIR="$RESULTS_DIR"
^----------------^ SC2034 (warning): KASEKI_RESULTS_DIR appears unused. Verify use (or export if used externally).


In ./tests/pr-metadata-generation.test.sh line 182:
TASK_PROMPT=''
^---------^ SC2034 (warning): TASK_PROMPT appears unused. Verify use (or export if used externally).


In ./tests/pr-metadata-generation.test.sh line 270:
VALIDATION_EXIT=0
^-------------^ SC2034 (warning): VALIDATION_EXIT appears unused. Verify use (or export if used externally).


In ./tests/pr-metadata-generation.test.sh line 439:
KASEKI_PUBLISH_MODE='draft_pr'
^-----------------^ SC2034 (warning): KASEKI_PUBLISH_MODE appears unused. Verify use (or export if used externally).


In ./tests/restore-disallowed-changes.test.sh line 12:
. "$ROOT_DIR/scripts/allowlist-helper.sh"
  ^-- SC1091 (info): Not following: ../scripts/allowlist-helper.sh: openBinaryFile: does not exist (No such file or directory)


In ./tests/restore-disallowed-changes.test.sh line 58:
KASEKI_CHANGED_FILES_ALLOWLIST='allowed.txt'
^----------------------------^ SC2034 (warning): KASEKI_CHANGED_FILES_ALLOWLIST appears unused. Verify use (or export if used externally).


In ./test/kaseki-api.integration.test.sh line 34:
    [[ -d "$RESULTS_DIR" ]] && find "$RESULTS_DIR" -maxdepth 3 -type f | sed 's/^/[artifact] /' || true
                            ^-- SC2015 (info): Note that A && B || C is not if-then-else. C may run when A is true.


In ./test/validation-fix.test.sh line 22:
  if grep -q 'bash -lc "\$trimmed"' "$REPO_ROOT/kaseki-agent.sh"; then
             ^--------------------^ SC2016 (info): Expressions don't expand in single quotes, use double quotes for that.


In ./test/validation-fix.test.sh line 57:
  local tmpdir exit_code
               ^-------^ SC2034 (warning): exit_code appears unused. Verify use (or export if used externally).


In ./test/printf-safety-focused.test.sh line 12:
source <(sed -n '/^# Validate that a variable/,/^}/p' "$SCRIPT_DIR/kaseki-agent.sh")
       ^-- SC1090 (warning): ShellCheck can't follow non-constant source. Use a directive to specify location.


In ./test/printf-safety-focused.test.sh line 80:
source <(sed -n '/^# Safely encode value/,/^}/p' "$SCRIPT_DIR/kaseki-agent.sh" | head -20)
       ^-- SC1090 (warning): ShellCheck can't follow non-constant source. Use a directive to specify location.


In ./test/printf-safety.test.sh line 14:
YELLOW='\033[1;33m'
^----^ SC2034 (warning): YELLOW appears unused. Verify use (or export if used externally).


In ./test/printf-safety.test.sh line 26:
  cd "$TEST_RESULTS_DIR"
  ^--------------------^ SC2164 (warning): Use 'cd ... || exit' or 'cd ... || return' in case cd fails.

Did you mean: 
  cd "$TEST_RESULTS_DIR" || exit


In ./test/printf-safety.test.sh line 42:
    printf "${GREEN}PASS${NC}\n"
           ^-------------------^ SC2059 (info): Don't use variables in the printf format string. Use printf '..%s..' "$foo".


In ./test/printf-safety.test.sh line 46:
    printf "${RED}FAIL${NC}\n"
           ^-----------------^ SC2059 (info): Don't use variables in the printf format string. Use printf '..%s..' "$foo".


In ./test/printf-safety.test.sh line 53:
  source <(sed -n '/^validate_numeric()/,/^}/p' "$KASEKI_SCRIPT")
         ^-- SC1090 (warning): ShellCheck can't follow non-constant source. Use a directive to specify location.


In ./test/printf-safety.test.sh line 57:
  source <(sed -n '/^generate_restoration_report()/,/^}/p' "$KASEKI_SCRIPT" | sed "s#/results#${PWD}/results#g")
         ^-- SC1090 (warning): ShellCheck can't follow non-constant source. Use a directive to specify location.


In ./test/printf-safety.test.sh line 222:
  source <(sed -n '/^json_encode()/,/^}/p' "$KASEKI_SCRIPT")
         ^-- SC1090 (warning): ShellCheck can't follow non-constant source. Use a directive to specify location.


In ./test/printf-safety.test.sh line 223:
  local output=$(printf 'test' | json_encode)
        ^----^ SC2155 (warning): Declare and assign separately to avoid masking return values.


In ./test/printf-safety.test.sh line 229:
  source <(sed -n '/^json_encode()/,/^}/p' "$KASEKI_SCRIPT")
         ^-- SC1090 (warning): ShellCheck can't follow non-constant source. Use a directive to specify location.


In ./test/printf-safety.test.sh line 235:
  local output=$(printf 'test' | json_encode 2>/dev/null || true)
        ^----^ SC2155 (warning): Declare and assign separately to avoid masking return values.


In ./test/printf-safety.test.sh line 290:
    printf "${GREEN}✓ All tests passed!${NC}\n"
           ^-- SC2059 (info): Don't use variables in the printf format string. Use printf '..%s..' "$foo".


In ./test/printf-safety.test.sh line 294:
    printf "${RED}✗ Some tests failed${NC}\n"
           ^-- SC2059 (info): Don't use variables in the printf format string. Use printf '..%s..' "$foo".


In ./test/validation-integration.test.sh line 7:
REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
^-------^ SC2034 (warning): REPO_ROOT appears unused. Verify use (or export if used externally).


In ./test/validation-integration.test.sh line 60:
  local tmpdir validation_log exit_code
                              ^-------^ SC2034 (warning): exit_code appears unused. Verify use (or export if used externally).


In ./test/actual-model-metadata.test.sh line 88:
  node -e 'const fs=require("node:fs");const m=JSON.parse(fs.readFileSync("/results/metadata.json","utf8"));if(m.actual_model!==process.argv[1])throw new Error(`expected ${process.argv[1]} got ${m.actual_model}`);' "$expected_model"
          ^-- SC2016 (info): Expressions don't expand in single quotes, use double quotes for that.


In ./test/actual-model-metadata.test.sh line 93:
    ! grep -q 'model_attribution_missing' /results/progress.jsonl
    ^-- SC2251 (info): This ! is not on a condition and skips errexit. Use `&& exit 1` instead, or make sure $? is checked.

Did you mean: 
     grep -q 'model_attribution_missing' /results/progress.jsonl && exit 1

For more information:
  https://www.shellcheck.net/wiki/SC1090 -- ShellCheck can't follow non-const...
  https://www.shellcheck.net/wiki/SC2034 -- ACTUAL_MODEL appears unused. Veri...
  https://www.shellcheck.net/wiki/SC2155 -- Declare and assign separately to ...
