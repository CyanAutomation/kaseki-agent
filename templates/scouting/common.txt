## [STRONG TEST_IMPACT EXAMPLES]

Use compact, concrete before/after examples only when they match the task. Each example has type, before, after, pattern, and description; use at most 5 test_examples per file.
- Parser change: {"path":"tests/parser.test.ts","reason":"parseRole() now handles null with fallback","test_examples":[{"type":"added_assertion","pattern":"Null-coalescing","before":"expect(parseRole(null)).toThrow(NullReferenceError)","after":"expect(parseRole(null)).toEqual({name:'Unnamed',level:0})","description":"Null now valid with defaults"}]}
- Event change: {"path":"tests/event-handler.test.ts","reason":"Event emission now async with new timing","test_examples":[{"type":"modified_assertion","pattern":"Async timing","before":"await eventPromise; // resolves within 10ms","after":"await eventPromise; // resolves within 100ms","description":"Batching adds latency"}]}
- Serialization change: {"path":"tests/response.test.ts","reason":"Response fields now camelCase","test_examples":[{"type":"modified_assertion","pattern":"camelCase fields","before":"expect(response.status_code).toBe(200)","after":"expect(response.statusCode).toBe(200)","description":"Field names migrated"}]}

## [CRITICAL_CHANGE_EXPECTATIONS GUIDANCE]
- required_files: only files that need edits; pre-verify them. Relevant-but-correct files do not belong here.
- required_search_strings: literal post-change function names, config keys, or assertion text expected in the diff; do not include code that should be removed or moved.
- forbidden_empty_diff: true for change requests; false for read-only/no-op tasks.
- Omit uncertain expectations.

## [SUGGESTED_ALLOWLIST GUIDANCE]
- agent_patterns: globs for files the coding agent may modify.
- validation_patterns: globs for files validation may modify.
- Be specific; both arrays can be empty if scope is unclear.

Execution context and efficiency requirements:
- Timeouts: Scouting should complete within 2 minutes total. Avoid deep recursion, large git history, 1000+ file enumeration, and slow regex.
- Artifact size: Maximum JSON size: 50 KB. Prioritize important observations and truncate with "(...truncated for brevity)".
- Error handling: report unreadable or malformed files in observations, proceed with limited scope, and do not fail the entire scouting phase; adapt and continue.
